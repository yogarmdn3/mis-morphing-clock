Morphing Clock (AP Mode) - PlatformIO project


Build instructions:
1) Install PlatformIO (PC) or pip install platformio
2) Open project folder and run: pio run -e esp32dev
3) Firmware is at: .pio/build/esp32dev/firmware.bin

If using Termux you can also run: pip install platformio  then pio run -e esp32dev
